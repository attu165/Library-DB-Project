# 📚 Library Database Project

This project contains a basic **Library Management System Database Design** for an internship task.

## Files Included
- `create_tables.sql` → SQL script to create normalized database (3NF).
- `normalization_report.md` → Steps showing 1NF → 3NF.
- `ER-Diagram.png` → ER diagram image (uploaded).
- `sample_data.sql` → (optional) sample inserts and test queries.

## How to use
1. Run `create_tables.sql` in MySQL/Postgres.
2. Optionally run `sample_data.sql` to insert test data.
3. Use basic SELECT queries to verify tables.
